
name: Test

on:
  workflow_dispatch:
    inputs:
      context_id:
        description: 'Select CONTEXT_ID'
        required: true
        default: 'app1'
        type: string
        enum:
          - 'app1'
          - 'performance_dev'
  push:
    branches:
      - main
jobs:
  Test-deploy:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [dev, perf]
    env:
      CONTEXT_ID: ${{ github.event.inputs.context_id || 'performance_dev' }}
      PLATFORM_ID: ${{ matrix.platform }}
    
    steps:
      - name: 🎯 Checkout Code
        uses: actions/checkout@v3

      - name: 🛠 test workflows
        uses: MohcineTor/Github_Actions/.github/actions/test@main

      - uses: MohcineTor/Github_Actions/.github/actions/pull@main
        with:
          platforms: "dev,perf"
          simulator_image_docker_orlistofImage: simulator_simulator, simu
          simulator_version_orlistofversion: ASSET_v14.2.1_2023-10-26-15h23-27Z, asset_v12


          
      